dependencies:
  override:
    - npm install
    - bower install
  post:
    - grunt build

test:
  override:
    - grunt test

#deployment:
#  prod:
#    branch: master
#    heroku:
#      appname: nerdschool-deploy

deployment:
  staging:
    branch: master
    heroku:
      appname: foo-bar-123